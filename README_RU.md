# О проекте

Уже не базовый, но и не особо продвинутый [Flutter](https://flutter.dev/docs/get-started/install) проект. Попытка реализовать архитектуру`BLoC` при помощи подхода [Didier's Boelens](https://www.didierboelens.com/).

Хотя `BLoC` выглядит вполне цельно, однако приходится писать много шаблонного кода, и `Dart` как язык не всегда помогает *делать вещи проще* (однако он развивается – насколько я могу судить – в правильном направлении). Таким образом, любая архитектура лучше, чем её отсутствие. `BLoC` свою задачу выполняет, так же как `Dart`.

В приложении нет тяжёлых вычислений, поэтому не используется механизм `Isolates`. Но много асинхронного кода `async/await`.

### Контакты

- [Telegram](https://t.me/pinq_punq)
- [LinkedIn](#)

### Как запустить

Не нужны ни токены, ни какая-то особенная подготовка. Установите `Flutter` и компилируйте приложение. Протестировано на `Android` устройстве и `iOS` симуляторе.

### О пользовательском интерфейсе

Неоморфизм выглядит интересно и свежо, однако низкий контраст - это не круто. Попытки увеличить контраст увеличением теней выглядят слишком вычурно (как по мне). Таким образом, `тотальный неоморфизм` не приемлем (для меня (на данный момент)). Всё-таки в этом приложении я применил неоморфизм ко многим элементам. Получилось далеко от совершенства, но выглядит терпимо. Так что пусть будет. 

### О коде и архитектуре 

Используйте этот г**нокод на свой страх и риск. Не ждите увидеть в этом проекте `правильный способ` делать всякое. Однако если это поможет кому-то понять  `как не нужно`, я буду доволен 😉

# О копирайте

Все копирайты в [английской версии](README.md).