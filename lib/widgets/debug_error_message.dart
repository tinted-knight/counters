import 'package:flutter/material.dart';

class YouShouldNotSeeThis extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Error. If you see this, something has gone wrong. Sorry :("),
    );
  }
}
